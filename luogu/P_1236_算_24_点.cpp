#include<bits/stdc++.h>
#define ll long long

using namespace std;

int a[4], x[2][4];

void dfs(int depth, unordered_set<int> vis) {
    if (depth == 3) {
        int sum, cnt = 0;
        for (int i = 0; i < 4; i++) {
            if (!vis.count(a[i])) {
                x[]
            }
        }
    }
}

void solve() {
    cin >> a[0] >> a[1] >> a[2] >> a[3];
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);

    // freopen("xxx.in", "r", stdin);
    // freopen("xxx.out", "w", stdout);

    int T = 1; // cin >> T;
    while(T--) {
        solve();
    }

    return 0;
}